services:
  - type: web
    name: bhoomiai-ap
    env: python
    buildCommand: pip install -r requirements.txt && python app/data/ap_seed_data.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHONPATH
        value: .
